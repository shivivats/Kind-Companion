<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
    <item
        android:id="@+id/test_action_favorite"
        android:icon="@drawable/ic_favorite_black"
        android:title="Favorite_Test"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/test_action_settings"
        android:title="Settings_Test"
        app:showAsAction="never|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>
    -->

    <!-- so basically we need 4 main buttons: store as note, store in vault, cast into the void or burn -->
    <!-- back button would prompt a discard dialogue -->
    <!-- some of these buttons would prompt a dialogue as well -->

    <item
        android:id="@+id/action_store_note"
        android:icon="@drawable/ic_favorite_black"
        android:title="@string/store_note"
        app:showAsAction="ifRoom" /> <!-- change icon later -->

    <item
        android:id="@+id/action_store_vault"
        android:icon="@drawable/ic_favorite_black"
        android:title="@string/store_vault"
        app:showAsAction="ifRoom" /> <!-- change icon later -->

    <item
        android:id="@+id/action_send_void"
        android:icon="@drawable/ic_favorite_black"
        android:title="@string/send_void"
        app:showAsAction="ifRoom" /> <!-- change icon later -->

    <item
        android:id="@+id/action_burn_thoughts"
        android:icon="@drawable/ic_favorite_black"
        android:title="@string/burn_thoughts"
        app:showAsAction="ifRoom" /> <!-- change icon later -->

</menu>